version: '3.8'
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/app:ro
      - financial_tracker_data:/app/data

volumes:
  financial_tracker_data: {}
